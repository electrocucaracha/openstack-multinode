Category: Changed

The GitHub Super Linter dependency was updated from version 4.8.7 to 4.9.0, which may require migration steps if custom configurations are present. The update does not introduce any breaking behavior and maintains the existing API contract. This change is relevant for maintainers who need to review and potentially adjust their configuration settings to ensure compatibility with the new version.
