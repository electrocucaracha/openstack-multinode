Category: Added

The TravisCI cache now includes the vagrant boxes folder to speed up builds by reusing cached copies of downloaded images, reducing build time for maintainers who use Vagrant in their workflow. This change affects how builds are executed and may require no action from users, but they should be aware that large downloads will not occur on subsequent runs if a matching image is already cached. The .travis.yml file has been updated to include the necessary cache configuration.
