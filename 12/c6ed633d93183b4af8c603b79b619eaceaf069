Category: Added

Cinder volumes are now properly initialized by creating an empty volume before formatting it for use, addressing a potential issue where the volume might be created in an inconsistent state. This change ensures that cinder volumes are always empty and ready for use. The modification involves adding two lines to the node.sh script: one to create an empty file using dd, and another to ensure the volume is properly formatted with pvcreate.
