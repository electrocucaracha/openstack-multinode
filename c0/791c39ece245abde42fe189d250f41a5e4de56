Category: Changed

The reviewdog/action-misspell dependency was updated from version 1.15.0 to 1.16.0, which may introduce breaking behavior if the new version has changed configuration options or requires migration steps. The update does not affect the API or CLI contract and there is no security impact. This change affects users who rely on the reviewdog/action-misspell action in their workflows, specifically those using the spell-checking feature.
