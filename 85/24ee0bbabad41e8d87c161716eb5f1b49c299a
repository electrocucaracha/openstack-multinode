Category: Changed

The project switched from custom vagrant boxes to official ones, which affects the CI environment. The `pinned_vagrant_boxes.txt` file now references official boxes, and corresponding changes were made in `update_distros.sh` and `distros_supported.yml`. This change impacts the distro versions used for testing and may require updates to existing test environments.
