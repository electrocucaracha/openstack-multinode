Category: Added

The default branch for the super-linter has been added to the GitHub workflow, allowing users to specify which branch should be used by default when running linter checks. This change introduces no breaking behavior and requires no migration steps. The configuration schema has been updated with a new `DEFAULT_BRANCH` setting that defaults to the current head ref or ref name if unspecified.
