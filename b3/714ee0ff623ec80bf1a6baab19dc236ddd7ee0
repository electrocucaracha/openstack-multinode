Category: Changed

The CPU allocation for the Controller node was increased in the noha CI workflow to improve performance. This change affects the deployment of controller nodes, which now require 2 CPUs instead of 1. The config schema has been updated accordingly, but migration steps are not required as this is a new requirement rather than a breaking change.
