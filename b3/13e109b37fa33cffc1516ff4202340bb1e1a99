Category: Changed

The dependency on actions/checkout was updated from version 4.1.5 to 4.1.6, which is a minor patch update that does not introduce breaking behavior or migration requirements. The change affects multiple workflows using the checkout action, but no API or CLI contract changes are involved. No security impact or config schema changes were reported in the release notes for version 4.1.6 of actions/checkout.
