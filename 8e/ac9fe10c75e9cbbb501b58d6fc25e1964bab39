Category: Added

A set of base requirements was added for zed, including dependencies for ansible, kolla-ansible, and mitogen, which will be used across various platforms. The requirements are generated by pip-compile and include specific versions for each dependency to ensure consistency. This change does not introduce any breaking behavior or API changes but may require migration steps when updating from previous versions.
