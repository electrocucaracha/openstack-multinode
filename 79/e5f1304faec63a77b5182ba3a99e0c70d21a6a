Category: Changed

The reviewdog/action-misspell dependency was updated from version 1.14.0 to 1.14.1, which may introduce breaking behavior if the previous version's configuration has changed in this new release. The update does not affect the API or CLI contract and there is no security impact. This change primarily affects maintainers who need to review and potentially adjust their workflows for spell checking.
