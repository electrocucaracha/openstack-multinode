Category: Changed

On Demand CI jobs have been improved by changing the workflow triggers to only run on specific file changes, reducing unnecessary job runs. The `on` trigger now specifies paths for push and pull request events, allowing more targeted execution. This change does not introduce any breaking behavior or API/CLI contract changes, nor does it affect security or config schema.
