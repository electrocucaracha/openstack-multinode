Category: Changed

The supported vagrant boxes for Rocky Linux have been updated to use the new repository URL, breaking any existing configurations that relied on the old URL. The API contract and CLI remain unaffected, and no security concerns are introduced by this change. Additionally, the config schema has been updated in distros_supported.yml to reflect the new box name and version.
