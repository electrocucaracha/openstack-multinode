Category: Changed

The dependency on `actions/checkout` was updated from version 4.1.4 to 4.1.5, which may require migration steps in workflows that use this action. The update does not introduce any breaking behavior, API changes, security impact, or config schema changes. This change is primarily a maintenance update to ensure compatibility with the latest version of `actions/checkout`.
