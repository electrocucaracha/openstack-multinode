Category: Changed

The default list of images in kolla-build.ini has been updated to include opendaylight, which means users will now have access to this image when building their environment. This change does not introduce any breaking behavior and no migration steps are required. The config schema changes as a result of adding the new image to the default list.
