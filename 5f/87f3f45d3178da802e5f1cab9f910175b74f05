Category: Changed

The actions/cache dependency was updated from version 3.2.6 to 3.3.0, which affects workflows that use this action for caching. The update requires no breaking changes and does not alter the API or CLI contract. This change is a minor upgrade and should be compatible with existing configurations, but users may need to re-run their workflows to take advantage of new features or bug fixes introduced in version 3.3.0.
