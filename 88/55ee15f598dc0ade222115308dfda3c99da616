Category: Changed

The AIO CI matrix generation was fixed, which affects users who rely on this feature for automated testing. The change involves modifying the workflow file to append the generated matrix to the GitHub output instead of overwriting it. This adjustment ensures that subsequent steps can access and use the generated matrix as intended, without breaking any existing workflows or requiring manual updates.
