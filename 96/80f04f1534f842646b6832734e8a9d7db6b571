Category: Removed

The upgrade script no longer enables xtrace by default, which previously caused unnecessary tracing in debug mode. This change removes an extraneous instruction that was only executed when OS_DEBUG was set to true. The update should not require any migration steps and does not affect the API or CLI contract.
